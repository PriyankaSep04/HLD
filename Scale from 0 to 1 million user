Message Queue

Rabbit MQ

Exchanges

Direct

Routing key == Binding key. Queue based on the binding key will receive message

Fan out

All queues will receive msgs.


Topic

Exchange can send message to more than one queue.

Sharding databases

Row wise sharding
Column wise sharding.

Drawback
Lets say there are lot of ppl with name A lot of records on T1. Shard them again and again (form a tree).
tree problem solve -> consistent hashing.

if join issues
denormalize
